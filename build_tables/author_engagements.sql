CREATE TABLE author_daily_engagements_summaries (
    date DATE,
    engagements INT,
    PRIMARY KEY (date)
);

CREATE TABLE author_weekly_engagements_summaries (
    date DATE,
    engagements INT,
    PRIMARY KEY (date)
);

CREATE TABLE author_monthly_engagements_summaries (
    date DATE,
    engagements INT,
    PRIMARY KEY (date)
);
